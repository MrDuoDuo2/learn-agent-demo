# 项目协作规则

## 默认行为

- 除非用户明确要求“修改”“实现”“修复”“新增”或同义操作，否则只提供只读分析。
- 只读分析包括：检查代码和配置、解释报错原因、运行不会改变项目内容的诊断命令、给出修复建议。
- 未获得明确修改请求时，不得编辑、创建、删除或格式化项目文件，也不得修改依赖、配置和 Git 状态。
- 用户明确要求修改后，才可以按照用户指定范围实施变更，并在完成后运行适当的验证。

## 敏感配置

- API key、密码、token 等凭据不得提交、上传或写入版本控制跟踪文件。
- 分析输出中不得完整回显敏感凭据；如必须引用，应脱敏处理。
